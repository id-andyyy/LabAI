# 🤖🧪 LabAI

AI-ассистент ЛАБАЙ для выполнения лабораторных работ. Ведёт от задания до готового отчёта по ГОСТу — через пошаговую инструкцию, проверку результатов и генерацию отчёта.

Работает как набор скиллов (slash-команд) для [Claude Code](https://claude.ai/claude-code).

## ⚡ Как это работает

Весь процесс выполнения лабораторной разбит на этапы, каждый из которых вызывается командой:

```
/lab-setup → /lab-instructions → выполнение инструкции → /lab-report → /lab-improve → /lab-research → /lab-conclusion → /lab-recover (по желанию)
```

## 🛠 Команды

### ⚙️ /lab-setup
Первоначальная настройка. Задаёт вопросы о доступных устройствах (компьютеры, сеть) и пожеланиях к оформлению отчётов. Записывает ответы в `CLAUDE.md`, чтобы все остальные команды учитывали окружение пользователя.

**Когда вызывать:** один раз, перед первым использованием. Можно вызвать повторно, если окружение изменилось.

### 📝 /lab-instructions
Начало работы. Читает задание (`task.md`), изучает дополнительные материалы и создаёт `instructions.md` — подробную пошаговую инструкцию: какую программу открыть, что нажать, какую команду ввести. В нужных местах расставляет плейсхолдеры для скриншотов и результатов.

**Когда вызывать:** в самом начале, когда есть `task.md` с заданием.

### 🆘 /lab-help
Помощь при выполнении инструкции. Если что-то не работает, появилась ошибка или непонятен конкретный шаг — опиши проблему после команды. Ассистент ответит в чате и при необходимости обновит инструкцию (изменённые пункты помечаются символом ⚠️).

**Когда вызывать:** в процессе выполнения инструкции, при возникновении проблемы.

```
/lab-help у меня команда ping возвращает ошибку "Request timed out"
```

### 🔄 /lab-update
Системное обновление инструкции. Используется, когда `instructions.md` в целом устарел и расходится с реальной ситуацией (не одна конкретная ошибка, а множество шагов стали неактуальны).

**Когда вызывать:** если инструкция перестала соответствовать реальности.

### 📊 /lab-report
Проверка и генерация отчёта. Сначала проверяет, все ли результаты в `instructions.md` заполнены и корректны. Если есть пропуски или ошибки — сообщает о них. Если всё в порядке — создаёт `report.md`: подробный отчёт по ГОСТу с умозаключениями, выводами и плейсхолдерами для рисунков.

**Когда вызывать:** после того как выполнил все шаги инструкции и заполнил результаты.

### ✨ /lab-improve
Доработка отчёта. Опиши, что не устраивает — нужно добавить, убрать, расписать подробнее или сократить. Можно вызывать многократно.

**Когда вызывать:** после получения `report.md`, если нужны правки.

```
/lab-improve раздел про DNS слишком короткий, распиши подробнее результаты трассировки
```

### 🔬 /lab-research
Добавляет в отчёт раздел дополнительного исследования. Укажи тему — ассистент разберётся, нужны ли дополнительные действия от тебя.

- Если исследование требует выполнения команд — ассистент добавит шаги в `instructions.md` (помечены ⚠️), после чего нужно выполнить их и снова запустить `/lab-research`. Если данных не хватает или они некорректны — ассистент обновит инструкцию и попросит доработать.
- Если дополнительных действий не нужно — ассистент сразу напишет аналитический раздел в `report.md`.

**Когда вызывать:** после `/lab-report`, когда основной отчёт готов.

```
/lab-research сравнение протоколов TCP и UDP при передаче мультимедиа
```

### 🏁 /lab-conclusion
Пишет итоговый вывод по всей работе — основной части и дополнительному исследованию. Объём ~пол страницы А4: что сделано, что выяснено, какие особенности замечены. Без воды и повторения отчёта по кругу. Если раздела дополнительного исследования в `report.md` ещё нет — скилл откажется выполняться и попросит сначала запустить `/lab-research`.

**Когда вызывать:** в самом конце, после `/lab-research`.

### 🧹 /lab-recover
Анализирует `instructions.md` и говорит, что нужно сделать, чтобы вернуть компьютер в исходное состояние: удалить созданные файлы, отключить включённые службы, откатить изменённую конфигурацию. Если конфигурация системы не менялась или откат уже был описан в инструкции — так и скажет. Ответ — в чате, кратко, только конкретные действия.

**Когда вызывать:** по желанию, после завершения работы.

## 🚀 Быстрый старт

1. `/lab-setup` — настраиваешь окружение (устройства, пожелания)
2. Положи задание в `task.md`
3. `/lab-instructions` — получаешь инструкцию
4. Выполняешь инструкцию, заполняешь результаты в `instructions.md`
5. При проблемах — `/lab-help`
6. `/lab-report` — получаешь отчёт
7. `/lab-improve` — дорабатываешь по вкусу
8. `/lab-research` — добавляешь доп. исследование
9. `/lab-conclusion` — пишешь итоговый вывод
10. `/lab-recover` — узнаёшь, что откатить в системе (по желанию)

## 📁 Структура проекта

```
├── CLAUDE.md              # Правила оформления (ГОСТ, плейсхолдеры, периферия)
├── task.md                # Задание лабораторной работы
├── instructions.md        # Пошаговая инструкция (создаётся через /lab-instructions)
├── report.md              # Отчёт (создаётся через /lab-report)
└── .claude/
    └── skills/            # Скиллы (slash-команды)
        ├── lab-setup/     # Настройка окружения
        ├── lab-instructions/
        ├── lab-help/
        ├── lab-update/
        ├── lab-report/
        ├── lab-improve/
        ├── lab-research/
        ├── lab-conclusion/
        └── lab-recover/
```

## 📋 Требования

- [Claude Code](https://claude.ai/claude-code)
- Или любая другая нейросеть, поддерживающая скиллы/команды
